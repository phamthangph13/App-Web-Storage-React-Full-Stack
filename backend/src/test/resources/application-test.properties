# Test Configuration
spring.profiles.active=test

# Server Configuration
server.port=0

# MongoDB Configuration (Embedded for testing)
spring.data.mongodb.uri=mongodb://localhost:27017/appp2p_auth_test
spring.data.mongodb.auto-index-creation=true

# JWT Configuration
app.jwt.secret=testSecretKey123456789012345678901234567890
app.jwt.expiration=3600000
app.jwt.refresh-expiration=7200000

# Email Configuration (Mock for testing)
spring.mail.host=localhost
spring.mail.port=25
spring.mail.username=test@example.com
spring.mail.password=testpassword

# Application Configuration
app.name=AppP2P-Test
app.frontend.url=http://localhost:3000
app.reset-password.expiration=3600000

# Logging
logging.level.com.appp2p=INFO
logging.level.org.springframework.security=WARN
logging.level.org.springframework.web=WARN